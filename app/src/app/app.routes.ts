import { Routes } from '@angular/router';
import { MoviesComponent } from './movies/movies.component.js';

export const routes: Routes = [
   
    {
        path: 'peliculas',
        component: MoviesComponent
    }
];
